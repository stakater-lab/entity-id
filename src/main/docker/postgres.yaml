version: '2'
services:
    nextval-demo-postgresql:
        image: postgres:10.4
        # volumes:
        #     - ~/volumes/jhipster/jhipsterSampleMicroservice/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=stakater
            - POSTGRES_PASSWORD=
            - POSTGRES_DB=vehicle
        ports:
            - 5432:5432